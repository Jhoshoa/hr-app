{
  "name": "@hr-app/geo",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No geo package lint yet\"",
    "test": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@hr-app/timezones": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
